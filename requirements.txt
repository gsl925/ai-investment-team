# Investment helper — Python dependencies
# Python 3.14+
#
# Setup:
#   py -3.14 -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt

# ── Core (required) ──────────────────────────────────────────────────────────
requests==2.34.2
certifi==2026.6.17
charset-normalizer==3.4.8
idna==3.18
urllib3==2.7.0

# ── LLM providers (optional — needed when settings.json llm.enabled=true) ────
# Set settings.json "provider" to "anthropic" or "openai" before installing.
anthropic==0.116.0
openai==2.44.0

# ── Transitive dependencies (pinned for reproducibility) ─────────────────────
annotated-types==0.7.0
anyio==4.14.1
colorama==0.4.6
distro==1.9.0
docstring_parser==0.18.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
jiter==0.16.0
pydantic==2.13.4
pydantic_core==2.46.4
sniffio==1.3.1
tqdm==4.68.3
typing-inspection==0.4.2
typing_extensions==4.16.0
